name: presentation
description: Presentation layer.
version: 0.0.1

publish_to: 'none'

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  data:
    path: ../data
  bloc: ^8.0.2
  flutter_bloc: ^8.0.1
  http_auth: ^1.0.1
  lazy_load_scrollview: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0